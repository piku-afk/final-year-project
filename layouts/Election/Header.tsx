import { Header as MantineHeader } from '@mantine/core';
import { CustomContainer } from 'components/CustomizeMantine';

export const ElectionHeader = () => {
  return (
    <MantineHeader height='auto'>
      <CustomContainer>hello</CustomContainer>
    </MantineHeader>
  );
};
